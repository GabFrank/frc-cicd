# Sensitive data — never commit
.env
notifications/.env
*.pem
*.key
*.pfx
*.jks
*.pepk

# Dashboard runtime artifacts (contienen credentials en plaintext)
dashboard/config-snapshot.json
dashboard/dev.db*
frc-dashboard-config-*.json

# Claude Code local config (contains SSH passwords in permission rules)
.claude/settings.local.json

# Helper local de SSH a hosts de prod (lee credenciales del .env)
frcssh.sh

# frc-comercial (solo parte del workspace, cada componente tiene su propio repo)
frc-comercial/

# OS
.DS_Store


# TypeScript incremental build cache
dashboard/tsconfig.tsbuildinfo
